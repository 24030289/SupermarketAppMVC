<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Supermarket App</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body, html {
      margin: 0;
      padding: 0;
      height: 100%;
      font-family: Arial, sans-serif;
    }

    .hero {
      background: url('/images/home-bg.png') no-repeat right center / cover;
      height: 100vh;
      display: flex;
      align-items: center;
      padding-left: 8%;
      color: #333;
    }

    .hero h1 {
      font-size: 3.4rem;
      font-weight: bold;
    }

    .hero p {
      font-size: 1.2rem;
      margin-bottom: 25px;
    }
  </style>
</head>

<body>

  <%- include("partials/navbar", { user: user }) %>

  <div class="hero">
    <div>
      <h1>Welcome to Supermarket App! </h1>
      <p>Your one-stop shop for fresh groceries & daily essentials.</p>

      <% if (!user) { %>
        <a href="/login" class="btn btn-dark btn-lg me-2">Login</a>
        <a href="/register" class="btn btn-outline-dark btn-lg">Register</a>
      <% } else { %>
        <% if (user.role === "admin") { %>
          <a href="/inventory" class="btn btn-success btn-lg">Manage Inventory</a>
        <% } else { %>
          <a href="/shopping" class="btn btn-success btn-lg">Start Shopping</a>
        <% } %>
      <% } %>
    </div>
  </div>

</body>
</html>
